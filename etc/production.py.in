from project.settings import *

ADMINS = (
    ('SeverAdmin', '@SERVER_ADMIN@'),
)
MANAGERS = ADMINS

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'OPTIONS': {
            'init_command': 'SET storage_engine=INNODB',
            'read_default_file': os.path.join(BUILDOUT_DIR, 'etc', 'my.cnf'),
        },
    }
}
